import { style } from '@vanilla-extract/css';

export const colliderInfoStyle = style({
  display: 'flex',
  flexDirection: 'row',
  flexWrap: 'wrap',
  alignItems: 'center',
  padding: '1rem',
  borderRadius: '10px',
});
